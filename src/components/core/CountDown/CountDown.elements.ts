import styled from 'styled-components'

export const CountDownWrap = styled.div`
  color: currentColor;
  font-size: inherit;
  line-height: inherit;
  font-family: inherit;
  letter-spacing: inherit;
`
