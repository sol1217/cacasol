import { defaultTheme } from '@styles'

export const focusStyles = `background-color: ${defaultTheme.palette.input.registration.code.backgroundPress}; border-color: ${defaultTheme.palette.input.registration.code.strokePress}; outline: none;`

export const defaultStyles = `border-color: ${defaultTheme.palette.input.registration.code.strokeDefault};  background-color: ${defaultTheme.palette.input.registration.code.backgroundDefault};`
